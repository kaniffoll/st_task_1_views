<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/Posts">

    <fragment
        android:id="@+id/Posts"
        android:name="com.example.st_task_1_views.PostsListView"
        android:label=""
        tools:layout="@layout/fragment_posts_list" >
        <action
            android:id="@+id/action_postsList_to_postScreen"
            app:destination="@id/postScreen" />
    </fragment>
    <fragment
        android:id="@+id/postScreen"
        android:name="com.example.st_task_1_views.PostScreenView"
        android:label="" >
        <action
            android:id="@+id/action_postScreen_to_postsList"
            app:destination="@id/Posts" />
        <argument
            android:name="post"
            app:argType="com.example.st_task_1_views.data.dataclasses.Post" />
    </fragment>
    <fragment
        android:id="@+id/Photos"
        android:name="com.example.st_task_1_views.AlbumsListView"
        android:label=""
        tools:layout="@layout/fragment_albums_list" />
</navigation>